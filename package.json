{
  "name": "rota-certa",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check app.js && node --check scripts/migrate-to-postgres.js && node --check scripts/migrate-admin-modules.js && node --check scripts/migrate-multicompany.js",
    "migrate:admin": "node scripts/migrate-admin-modules.js",
    "migrate:companies": "node scripts/migrate-multicompany.js"
  },
  "engines": {
    "node": ">=22.5"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "pg": "^8.23.0"
  }
}
